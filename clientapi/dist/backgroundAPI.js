// Generated by CoffeeScript 1.9.3
(function() {
  var EVENTS, SERVER_URL, io, webSocket;

  io = require('socket.io-client');

  EVENTS = require('./events.json');

  SERVER_URL = process.argv[process.argv.length - 1];

  webSocket = io.connect(SERVER_URL, {secure: true});

  webSocket.on(EVENTS.FrontConnected, function(data) {
    console.log('websocket event:'+data);
    return process.send({
      event: EVENTS.FrontConnected,
      data: data
    });
  });

  process.on('message', function(cmd) {
    return webSocket.emit(cmd);
  });

}).call(this);
